# 医药电商数据系统 - 架构设计方案

## 一、设计起点：先问对问题

### 1.1 业务本质

```
你的生意 = 卖药

卖药的核心链路：
  采购入库 -> 仓库存储 -> 接单 -> 出库发货 -> 售后退款

每个环节都在产生数据，数据在 Oracle ERP 里。
你需要的不是"数仓"，是"看清生意"。
```

### 1.2 三个核心问题

| 问题 | 本质 | 频率 |
|------|------|------|
| **今天赚了多少钱？** | 实时监控 | 分钟级 |
| **哪里出了问题？** | 异常发现 | 分钟级 |
| **趋势怎么看？** | 分析决策 | 日/周/月 |

### 1.3 用户画像

```
┌─────────────────────────────────────┐
│  代总（Boss）                        │
│  - 不写 SQL                          │
│  - 看钉钉多维表格 / 群消息            │
│  - 偶尔问 OpenClaw "今天怎么样"       │
│  - 关心数字，不关心技术               │
├─────────────────────────────────────┤
│  运营/业务员                          │
│  - 看钉钉报表                        │
│  - 需要具体单号、客户名、商品名        │
│  - 关心自己的业绩指标                 │
├─────────────────────────────────────┤
│  OpenClaw（AI）                      │
│  - 被问问题时查 DuckDB               │
│  - 异常时主动推送告警                 │
│  - 需要 schema 感知 + 安全护栏        │
└─────────────────────────────────────┘
```

---

## 二、分层架构设计

```
                    ┌─────────────────────────────────┐
                    │         消费层 (Consumption)      │
                    │                                   │
                    │  钉钉多维表格    OpenClaw NL查询   │
                    │  钉钉群告警      定时报表推送      │
                    └──────────────┬──────────────────┘
                                   │
                    ┌──────────────▼──────────────────┐
                    │         服务层 (Service)          │
                    │                                   │
                    │  KPI 计算引擎    告警判定引擎      │
                    │  报表生成器      NL->SQL 翻译器    │
                    └──────────────┬──────────────────┘
                                   │
                    ┌──────────────▼──────────────────┐
                    │         存储层 (Storage)          │
                    │                                   │
                    │  DuckDB (热)     Parquet (冷)     │
                    │  raw -> agg -> dim 三层模型       │
                    └──────────────┬──────────────────┘
                                   │
                    ┌──────────────▼──────────────────┐
                    │         采集层 (Ingestion)        │
                    │                                   │
                    │  CDC 增量同步    全量初始化        │
                    │  数据校验        错误重试          │
                    └──────────────┬──────────────────┘
                                   │
                    ┌──────────────▼──────────────────┐
                    │         数据源 (Source)           │
                    │                                   │
                    │  Oracle 12c 时空 ERP (RACE)      │
                    │  27 张表 · READONLY_DB_USER 只读 · 12.1.0.2  │
                    └─────────────────────────────────┘
```

---

## 三、每层的设计决策与理由

### 3.1 采集层

#### 核心选择：时间戳轮询 CDC

**为什么不用其他方案？**

| 方案 | 适合场景 | 不适合我们的原因 |
|------|----------|------------------|
| LogMiner | 需要秒级延迟 | Oracle 12c LogMiner 配置复杂，READONLY_DB_USER 是只读账号无权限 |
| GoldenGate | 企业级实时 | 太贵，一个 License 够买 10 台 Mac mini |
| 触发器 | 强一致 | 需要写权限，ERP 厂商不允许改表 |
| 物化视图 | 中等 | 需要 DBA 配合，运维成本高 |
| **时间戳轮询** | **分钟级延迟** | **零侵入，只读就能做，足够用** |

**时间戳轮询的工程要点：**

```
关键问题：Oracle 时间字段是 CHAR 类型，不是 TIMESTAMP

设计决策：
  1. 采集时保持 CHAR 原样传入 DuckDB（不做转换）
  2. 在 DuckDB 里用 TRY_CAST 或 strptime 转换
  3. CDC 水位线用字符串比较（CHAR 格式 YYYY-MM-DD HH:MM:SS 天然支持字符串排序）

关键问题：明细表没有独立时间字段

设计决策：
  1. 主表增量查出新增/变更的 BILLNO 列表
  2. 用 BILLNO 反查明细表
  3. 反查结果做 upsert
  4. 延迟可接受（主表先到，明细晚一个批次）
```

**增量 vs 全量的边界：**

```
全量同步（DELETE + INSERT）：
  - 首次初始化
  - 数据修复/重建
  - 小表（<1000行）的定时全量刷新（比增量更简单可靠）
  - 日级同步的表（STOREHOUSE 9行、STAFFDOC 60行）

增量同步（WHERE timestamp > 水位线）：
  - 大表（SALEOUTMT 65万、SALENOTESDT 86万）
  - 高频同步（5分钟/15分钟/1小时）
  - 有可靠时间字段的表
```

#### 写入优化（当前逐行插入的改进方向）

```
当前方案：逐行 execute（慢但稳）
  - 65万行 SALEOUTMT 需要 ~17分钟
  - 优点：不会因为单行类型错误中断整批
  - 缺点：慢

优化方案（分阶段）：
  Phase 1（现在）：逐行插入，可接受
  Phase 2（优化）：DuckDB Appender API，10x 提速
    import duckdb
    appender = conn.appender('raw', 'SALEOUTMT')
    for row in rows:
        appender.append_row(row)
    appender.flush()
  Phase 3（终极）：PyArrow + DuckDB 零拷贝
    import pyarrow as pa
    table = pa.Table.from_pandas(df)
    conn.register('temp_table', table)
    conn.execute('INSERT INTO raw.SALEOUTMT SELECT * FROM temp_table')
```

#### 数据校验（当前缺失，需要补）

```
每次同步后自动校验：
  ┌──────────────────────────────────────┐
  │  校验项              │ 判定标准       │
  ├──────────────────────┼───────────────┤
  │  Oracle 行数          │ 源端 COUNT(*) │
  │  DuckDB 行数          │ 目标 COUNT(*) │
  │  差异行数             │ |源 - 目标|   │
  │  关键字段 NULL 率     │ < 5%          │
  │  金额合计对比         │ 差异 < 0.01%  │
  │  主键重复检查          │ 0            │
  └──────────────────────┴───────────────┘

异常处理：
  - 差异 > 阈值 -> 写入 agg.data_audit + 告警
  - 连续 3 次失败 -> 暂停该表增量，等人工介入
```

---

### 3.2 存储层

#### 三层模型

```
┌─────────────────────────────────────────────────────────┐
│  raw 层 -- 原始镜像                                      │
│                                                         │
│  设计原则：                                               │
│  1. 列名与 Oracle 完全一致（大写）                        │
│  2. 类型做安全映射（CHAR->VARCHAR, NUMBER->DECIMAL）      │
│  3. 不做任何业务转换                                      │
│  4. 加 _etl_time + _etl_source 元数据列                  │
│  5. 主键表加 PRIMARY KEY 约束（自动去重）                 │
│                                                         │
│  作用：可重放的数据底座，任何时候都能从 raw 重建 agg/dim   │
├─────────────────────────────────────────────────────────┤
│  dim 层 -- 维度表                                        │
│                                                         │
│  设计原则：                                               │
│  1. 从 raw 层 JOIN 多表清洗而来                          │
│  2. 只保留分析需要的字段（不是全部 280 列）               │
│  3. 做命名规范化（GOODSNAME -> product_name）             │
│  4. 加分类标签（OTC/RX、医保/非医保、内贸/外贸）          │
│                                                         │
│  核心表：                                                 │
│  dim_product  -- 商品（GOODSDOC + GOODSATTR 关键字段）   │
│  dim_customer -- 客户（CLIENTDOC + BUSINESSDOC）         │
│  dim_warehouse -- 仓库                                   │
│  dim_supplier  -- 供应商                                 │
│  dim_staff     -- 员工                                   │
├─────────────────────────────────────────────────────────┤
│  agg 层 -- 聚合结果                                      │
│                                                         │
│  设计原则：                                               │
│  1. 预计算，消费层直接读                                  │
│  2. 按时间粒度分表（实时/日/周/月）                       │
│  3. 按维度分表（销售/采购/库存/退款）                     │
│  4. 保留同比/环比字段（预计算而非查询时算）               │
│                                                         │
│  核心表：                                                 │
│  agg.kpi_realtime      -- 当日实时 KPI（分钟级更新）      │
│  agg.daily_sales       -- 日销售汇总（按日+仓库+客户类型）│
│  agg.daily_product     -- 日商品销售排行                  │
│  agg.alert_inventory   -- 库存异常记录                    │
│  agg.weekly_report     -- 周报                           │
│  agg.monthly_report    -- 月报                           │
│  agg.data_audit        -- 数据对账记录                    │
└─────────────────────────────────────────────────────────┘
```

#### 为什么不用更多层（ODS/DWD/DWS/ADS）？

```
经典数仓分 4 层是给大型团队的：
  ODS -> DWD -> DWS -> ADS
  每层一个团队维护，层间解耦

你的场景：
  - 1 个人用（OpenClaw 帮你）
  - 1 个数据源（Oracle ERP）
  - 数据量不大（300万行核心数据）
  - 不需要跨部门数据集市

3 层够用，多了是负担。
等业务涨 10 倍再考虑加层。
```

#### Parquet 冷存储

```
策略：
  - raw 层数据保留 90 天在 DuckDB（热）
  - 超过 90 天的导出为 Parquet 文件（冷）
  - DuckDB 可以直接查询 Parquet 文件（ATTACH 或直接读）
  - 需要历史数据时：DuckDB UNION Parquet

文件组织：
  data/parquet/
    SALEOUTMT/
      2026-04.parquet
      2026-05.parquet
      2026-06.parquet
    SALEOUTDT/
      2026-04.parquet
      ...

查询示例：
  -- 查 4 月数据（从 Parquet）
  SELECT * FROM read_parquet('data/parquet/SALEOUTMT/2026-04.parquet')
  UNION ALL
  -- 查近期数据（从 DuckDB）
  SELECT * FROM raw.SALEOUTMT WHERE DATES >= '2026-05-01'
```

#### DuckDB 性能特性利用

```
DuckDB 的优势在于分析查询，不需要像 SQLite 那样调优：

1. 列式存储 -> 只读需要的列，速度天然快
2. 向量化执行 -> 聚合查询（SUM/COUNT/GROUP BY）亚秒级
3. 零拷贝 Arrow 集成 -> Python pandas 互转无开销
4. 内存管理 -> 自动 spill to disk，不爆内存

需要做的优化：
  - 大表建索引（CREATE INDEX idx_billno ON raw.SALEOUTMT(BILLNO)）
  - 定期 VACUUM（清理删除后的碎片）
  - 不要在高峰期做全量重建
```

---

### 3.3 服务层

这一层是"连接数据和消费的桥梁"，当前代码里缺失最多。

#### KPI 计算引擎

```python
"""
设计思路：
  - 不是简单的 SELECT SUM，而是带同比/环比/目标对比
  - 结果写入 agg.kpi_realtime，消费层直接读
  - 每 5 分钟刷新一次
"""

# KPI 定义（配置化，不是硬编码）
KPI_DEFS = {
    "sales_amount": {
        "name": "销售额(含税)",
        "sql": "SELECT SUM(TAXAMOUNT) FROM raw.SALEOUTMT WHERE TRY_CAST(DATES AS DATE) = CURRENT_DATE",
        "unit": "元",
        "target": 100000,  # 日目标，可配置
        "compare": "yoy+mom",  # 同比+环比
    },
    "order_count": {
        "name": "出库单数",
        "sql": "SELECT COUNT(*) FROM raw.SALEOUTMT WHERE TRY_CAST(DATES AS DATE) = CURRENT_DATE",
        "unit": "单",
        "target": 200,
        "compare": "yoy+mom",
    },
    "gross_profit": {
        "name": "毛利",
        "sql": "SELECT SUM(PROFIT) FROM raw.SALEOUTMT WHERE TRY_CAST(DATES AS DATE) = CURRENT_DATE",
        "unit": "元",
        "target": 30000,
        "compare": "yoy+mom",
    },
    "refund_amount": {
        "name": "退款金额",
        "sql": "SELECT SUM(REFUNDFEE) FROM raw.K_D3OMS_ORDERREFUNDMT WHERE TRY_CAST(LASTMODIFYTIME AS DATE) = CURRENT_DATE",
        "unit": "元",
        "target": 0,
        "compare": "yesterday",
        "alert": "value > 5000",  # 超过5000告警
    },
    "low_stock_count": {
        "name": "低库存商品数",
        "sql": "SELECT COUNT(*) FROM raw.ANGLEBALANCE WHERE PLACENUM < STORMIN AND STORMIN > 0",
        "unit": "个",
        "target": 0,
        "alert": "value > 10",
    }
}
```

#### 告警判定引擎

```
设计原则：
  1. 告警规则配置化（不硬编码在脚本里）
  2. 告警分级（INFO / WARNING / CRITICAL）
  3. 告警去重（同一问题 30 分钟内只报一次）
  4. 告警升级（CRITICAL 超过 1 小时未处理 -> 升级通知）

告警规则表（DuckDB 里的配置表）：
  ┌──────────────┬──────────┬─────────────────────────────────┬──────────┬──────────┐
  │ alert_name   │ severity │ condition                       │ cooldown │ channel  │
  ├──────────────┼──────────┼─────────────────────────────────┼──────────┼──────────┤
  │ 低库存       │ WARNING  │ PLACENUM < STORMIN              │ 60min    │ 钉钉群   │
  │ 断货         │ CRITICAL │ PLACENUM = 0 AND 有销售记录      │ 30min    │ 钉钉群+@  │
  │ 大额退款     │ WARNING  │ REFUNDFEE > 5000                │ 即时     │ 钉钉群   │
  │ 同步失败     │ CRITICAL │ CDC status = ERROR              │ 即时     │ 钉钉群   │
  │ 数据偏差     │ WARNING  │ audit diff > 0.1%               │ 即时     │ 钉钉群   │
  └──────────────┴──────────┴─────────────────────────────────┴──────────┴──────────┘
```

#### NL -> SQL 翻译器

```
这是 OpenClaw 的核心能力之一，设计要点：

1. Schema 注入
   - 给 AI 一份精简版 schema（不是全部 280 列）
   - 包含表名、关键字段、字段含义、表间关系
   - 包含业务术语映射（"销售额" = SALEOUTMT.TAXAMOUNT）

2. 查询模板
   - 高频问题用预定义模板，不走 AI 生成
   - "今天卖了多少" -> 直接命中模板
   - 复杂问题才走 AI 自由生成

3. 安全护栏
   - 只允许 SELECT，禁止 INSERT/UPDATE/DELETE/DROP
   - 限制查询行数（LIMIT 1000）
   - 超时 10 秒自动 kill
   - 敏感字段不返回（STAFFDOC.MOBILE 等）

4. 结果渲染
   - 数字 -> 表格
   - 趋势 -> 图表（用 DuckDB 的 SUMMARIZE 或前端渲染）
   - 明细 -> 可下载 CSV
```

---

### 3.4 消费层

#### 钉钉多维表格（给非技术人员看）

```
设计原则：
  1. 消费层不存原始数据，只存"已经算好的结果"
  2. 每张表 < 2万行（多维表格的舒适区）
  3. upsert 为主（保持"最新状态"），append 为辅（日志类）

8 张多维表格的设计：

┌────────────────────┬──────────┬──────┬───────────────────────────┐
│ 表名               │ 同步方式 │ 行数 │ 字段设计                   │
├────────────────────┼──────────┼──────┼───────────────────────────┤
│ 📊 KPI实时仪表盘   │ upsert   │ ~20  │ 指标名/今日值/昨日值/      │
│                    │          │      │ 同比%/环比%/目标/完成率    │
├────────────────────┼──────────┼──────┼───────────────────────────┤
│ 📅 日报_销售汇总   │ append   │ ~30/月│ 日期/仓库/订单数/         │
│                    │          │      │ 销售额/毛利/毛利率         │
├────────────────────┼──────────┼──────┼───────────────────────────┤
│ ⚠️ 异常_库存告警   │ append   │ <100 │ 日期/商品/仓库/当前库存/   │
│                    │          │      │ 阈值/告警类型/严重程度     │
├────────────────────┼──────────┼──────┼───────────────────────────┤
│ 📋 主数据_客户     │ upsert   │ ~500 │ 客户编号/名称/类型/        │
│                    │          │      │ 累计销售额/最近下单/状态   │
├────────────────────┼──────────┼──────┼───────────────────────────┤
│ 📦 主数据_商品     │ upsert   │ ~2000│ 商品编码/名称/规格/        │
│                    │          │      │ 月销量/月销售额/状态       │
├────────────────────┼──────────┼──────┼───────────────────────────┤
│ 📈 周报            │ append   │ ~4/月│ 周期/指标/本周值/          │
│                    │          │      │ 上周值/环比%               │
├────────────────────┼──────────┼──────┼───────────────────────────┤
│ 📊 月报            │ append   │ ~1/月│ 月份/指标/本月值/          │
│                    │          │      │ 上月值/同比%/环比%         │
├────────────────────┼──────────┼──────┼───────────────────────────┤
│ 🔍 数据对账        │ append   │ ~24/天│ 时间/表名/源端行数/        │
│                    │          │      │ 目标行数/差异/状态         │
└────────────────────┴──────────┴──────┴───────────────────────────┘
```

#### 钉钉群机器人（告警通道）

```
告警消息设计：

🔴 CRITICAL（红色卡片）：
  标题: 🚨 断货告警
  内容: 商品「阿莫西林胶囊」在「电商库B」已断货
        上次出库: 2026-07-21 14:30
        最近7天销量: 128盒
  操作: [查看详情] [创建采购单]

🟡 WARNING（黄色卡片）：
  标题: ⚠️ 大额退款
  内容: 订单 OM<示例订单号> 退款 ¥8,560
        退款原因: 商品质量问题
        客户: 武汉XX药房
  操作: [查看订单]

🟢 INFO（绿色卡片）：
  标题: 📊 每日 KPI 播报
  内容: 7月22日 销售额 ¥45,230 | 毛利 ¥8,120
        订单 156 单 | 退款 2 单 ¥320
        目标完成率: 45.2%
```

#### OpenClaw NL 查询（对话式）

```
典型对话：

用户: 今天卖了多少？
AI:   今天截至 11:30，销售出库 156 单，含税销售额 ¥45,230，
      毛利 ¥8,120（毛利率 17.9%）。目标完成率 45.2%。

用户: 这周比上周怎么样？
AI:   本周（7/15-7/22）累计销售额 ¥312,500，上周 ¥289,300，
      环比 +8.0%。主要增长来自电商库B（+15.3%）。

用户: 阿莫西林最近卖得好吗？
AI:   「阿莫西林胶囊」近 30 天销售 1,280 盒，¥38,400。
      在感冒类药品中排第 3。当前库存 85 盒，按近 7 天
      日均 43 盒的速度，预计 2 天后断货。

用户: 帮我看下哪些商品快过期了
AI:   查到 12 个商品批次将在 90 天内过期：
      1. 布洛芬片 批号 BK20250601 有效期 2026-10-01 库存 230盒
      2. ...
      需要我推送告警到钉钉群吗？
```

---

## 四、调度设计

### 4.1 调度架构

```
不用 Airflow / DolphinScheduler -- 太重。
不用 cron 直连 Python -- 太散。

设计：一个调度脚本 + crontab 触发

crontab（触发器）
  │
  ├─ */5min  -> scripts/run_cdc.sh sale    -> CDC增量（销售类）
  ├─ */15min -> scripts/run_cdc.sh oms     -> CDC增量（OMS类）
  ├─ */3min  -> scripts/run_alert.sh       -> 告警检查
  ├─ hourly  -> scripts/run_cdc.sh pur     -> CDC增量（采购类）
  ├─ 6hourly -> scripts/run_cdc.sh dim     -> CDC增量（维度类）
  ├─ daily   -> scripts/run_daily.sh       -> 日报+归档+对账
  └─ weekly  -> scripts/run_weekly.sh      -> 周报
```

### 4.2 容错设计

```
每个脚本的自愈逻辑：

1. 锁检测
   - 启动前检查 DuckDB 是否被锁
   - 如果被锁 -> 等待 30 秒 -> 仍锁则退出（下次 cron 会再触发）

2. 超时保护
   - 单次同步超时 5 分钟 -> 自动 kill
   - 防止 Oracle 慢查询卡死整个管道

3. 失败重试
   - 同步失败 -> 重试 1 次
   - 仍失败 -> 记录错误到 _cdc_state.status = 'ERROR'
   - 连续 3 次 ERROR -> 暂停该表 -> 告警

4. 幂等性
   - 全量同步: DELETE + INSERT（天然幂等）
   - 增量同步: upsert（先删后插，幂等）
   - 重复执行不会产生脏数据
```

---

## 五、数据质量体系

### 5.1 三层校验

```
┌──────────────────────────────────────────────────────┐
│  Layer 1: 采集时校验（CDC 脚本内置）                  │
│                                                      │
│  - Oracle 行数 vs DuckDB 行数                         │
│  - 主键重复检查                                       │
│  - 必填字段 NULL 检查                                 │
│  - 异常 -> 跳过该行 + 记录到日志                      │
├──────────────────────────────────────────────────────┤
│  Layer 2: 每小时对账（独立脚本）                      │
│                                                      │
│  - 逐表对比 Oracle COUNT(*) vs DuckDB COUNT(*)        │
│  - 金额合计对比                                       │
│  - 结果写入 agg.data_audit                           │
│  - 差异 > 阈值 -> 告警                                │
├──────────────────────────────────────────────────────┤
│  Layer 3: 每日全检（日报脚本内置）                    │
│                                                      │
│  - 近 30 天数据完整性                                 │
│  - 时间序列连续性（没有"丢天"）                       │
│  - 业务逻辑校验（销售额 > 0、毛利率 0-100%）          │
│  - 异常 -> 日报里标注 + 告警                          │
└──────────────────────────────────────────────────────┘
```

### 5.2 数据血缘

```
知道每个数字"从哪来"：

agg.kpi_realtime.sales_amount
  └─ raw.SALEOUTMT.TAXAMOUNT
       └─ Oracle RACE.SALEOUTMT.TAXAMOUNT
            └─ ERP 出库单据金额

当数字异常时，可以顺着血缘追溯：
  KPI 异常 -> 查 agg 层 SQL -> 查 raw 层数据 -> 查 Oracle 源头
```

---

## 六、安全设计

```
┌──────────────────────────────────────────────────┐
│  数据安全                                          │
│                                                    │
│  1. Oracle 密码不硬编码 -> config.yaml (gitignore) │
│  2. DuckDB 文件权限 600                            │
│  3. Parquet 归档加密（可选）                        │
│  4. 日志脱敏（手机号/身份证/银行卡）                │
├──────────────────────────────────────────────────┤
│  查询安全（NL -> SQL）                              │
│                                                    │
│  1. 只允许 SELECT                                   │
│  2. 敏感字段自动屏蔽（MOBILE/IDCARD/EMAIL）         │
│  3. 查询超时 10s                                    │
│  4. 结果行数限制 1000                               │
│  5. SQL 审计日志                                    │
├──────────────────────────────────────────────────┤
│  访问安全                                           │
│                                                    │
│  1. 钉钉多维表格按角色授权                           │
│  2. OpenClaw 继承当前用户权限                        │
│  3. 敏感操作需要确认（删除/重建/全量同步）           │
└──────────────────────────────────────────────────┘
```

---

## 七、演进路线

### Phase 1: 数据进来（Week 1-2）-- 当前阶段

```
✅ 环境搭建 (DuckDB + Python + DWS CLI)
✅ Oracle 探查 (27 表, 300万行)
✅ CDC 引擎 (full + incremental)
✅ DuckDB 三层 schema
🔄 全量同步所有表
⬜ crontab 调度配置
⬜ 数据对账脚本
```

### Phase 2: 数据用起来（Week 2-3）

```
⬜ KPI 计算引擎
⬜ 钉钉多维表格创建 + 字段映射
⬜ 数据推送脚本 (DuckDB -> 钉钉)
⬜ 库存告警脚本
⬜ 钉钉群机器人配置
⬜ 日报/周报生成器
```

### Phase 3: 智能化（Week 3-4）

```
⬜ OpenClaw NL 查询接入
  - schema 注入
  - 查询模板库
  - 安全护栏
⬜ 异常自动发现
  - 基于历史数据的异常检测
  - 趋势预测（简单移动平均）
⬜ 定时报表推送
  - 每日 8:00 KPI 播报到钉钉群
  - 每周一 8:30 周报推送
```

### Phase 4: 优化与扩展（Month 2+）

```
⬜ 性能优化
  - DuckDB Appender API 替代逐行插入
  - 大表建索引
  - Parquet 冷热分层

⬜ 更多数据源
  - 钉钉考勤/审批数据
  - 快递物流 API
  - 电商平台 API（淘宝/京东健康）

⬜ 高级分析
  - 客户 RFM 分群
  - 商品关联分析（购物篮）
  - 销售预测模型
  - 智能补货建议

⬜ 多租户
  - 不同部门看到不同数据
  - 业务员只看自己的客户
```

---

## 八、设计决策记录（ADR）

### ADR-001: 选择 DuckDB 而非 SQLite/PostgreSQL

```
背景: 需要一个本地分析型数据库
决策: DuckDB
理由:
  - 列式存储，分析查询（SUM/GROUP BY）比 SQLite 快 10-100x
  - 零配置，单文件，不需要跑服务
  - ARM 原生优化（Mac mini M4 Pro）
  - 内置 Parquet 读写
  - Python 集成好（pandas 互转零开销）
权衡:
  - 不适合高并发写入（单写锁）
  - 不适合做 OLTP（没有行锁）
  但我们的场景是批量写入 + 分析查询，完美匹配
```

### ADR-002: 选择时间戳轮询而非日志 CDC

```
背景: 需要从 Oracle 增量同步数据
决策: 时间戳轮询（LASTMODIFYTIME/SYSDATES）
理由:
  - READONLY_DB_USER 是只读账号，不能用 LogMiner
  - 不需要 DBA 配合
  - 分钟级延迟可接受
  - 实现简单，运维成本低
权衡:
  - 不能捕获物理删除（DELETE 操作不会被感知）
  - 延迟比日志 CDC 高（分钟级 vs 秒级）
  但 ERP 数据是"追加为主"的，删除极少，可接受
```

### ADR-003: 选择钉钉多维表格而非自建 Web 仪表盘

```
背景: 需要一个消费层让 Boss 看数据
决策: 钉钉多维表格 + 群机器人
理由:
  - Boss 已经在用钉钉，不需要装新 App
  - 多维表格支持筛选/排序/图表，够用
  - DWS CLI 提供完整的 API（41+命令）
  - 群机器人可以推告警
  - 不需要维护前端服务器
权衡:
  - 多维表格有行数限制（单表 < 2万行）
  - 图表能力不如专业 BI 工具
  但消费层只放"结果数据"（KPI/日报），不放明细，2万行足够
```

### ADR-004: 不引入 FastAPI / 中间件层

```
背景: 是否需要一个 API 服务层
决策: 不需要，Python 脚本直接跑
理由:
  - 消费者是钉钉（DWS CLI）和 OpenClaw，不是浏览器
  - 没有前端页面需要服务
  - crontab + Python 足够简单可靠
  - 少一个服务 = 少一个故障点
权衡:
  - 不能实时触发（只能定时跑）
  - 没有 API 接口给外部调用
  但分钟级的定时任务已经足够实时了
```

### ADR-005: 三层模型而非四层

```
背景: 数仓分层设计
决策: raw -> agg/dim 三层（不分 DWD/DWS）
理由:
  - 单数据源（Oracle ERP），不需要 ODS 做多源整合
  - 1 个人用，不需要层间解耦
  - 数据量小（300万行），不需要分层减少扫描
  - 3 层已经足够清晰：原始 -> 维度 -> 聚合
权衡:
  - 如果未来加多数据源，需要加 ODS 层
  - 如果团队扩大，层间耦合会成问题
  但当前阶段，简单就是好
```

---

## 九、总结：这套系统的设计哲学

```
1. 简单优先
   能用 Python 脚本解决的，不引入服务
   能用 crontab 调度的，不引入 Airflow
   能用 DuckDB 单文件的，不引入集群

2. 只读源头
   对 Oracle ERP 零侵入
   不改表、不加触发器、不动配置

3. 可重建
   raw 层是唯一的真相来源
   agg/dim 可以随时从 raw 重建
   DuckDB 可以随时从 Oracle 重建
   没有任何不可恢复的状态

4. 配置驱动
   CDC 规则、KPI 定义、告警规则都是配置
   改配置不改代码

5. 渐进式
   先跑通数据流（Phase 1）
   再建消费层（Phase 2）
   再加智能化（Phase 3）
   不追求一步到位
```
