-- ============================================
-- 补充建表 SQL - 9 张缺失表
-- 基于 Oracle RACE Schema 实际结构
-- 2026-07-24
-- ============================================

-- ============================================
-- raw.BUSINESSDOC - 业务主体表 (1.5万行, 105列)
-- ============================================
CREATE TABLE IF NOT EXISTS raw.BUSINESSDOC (
    BILLNO BIGINT,
    POSTCODE VARCHAR,
    BUSINESSID VARCHAR,
    ENTID VARCHAR,
    RFENTID VARCHAR,
    PBUSINESSID VARCHAR,
    BUSINESSCODE VARCHAR,
    BUSINESSNAME VARCHAR,
    SHORTNAME VARCHAR,
    LOGOGRAM VARCHAR,
    OLDCODE VARCHAR,
    TAXNO VARCHAR,
    CONTACT VARCHAR,
    TELEPHONE VARCHAR,
    URL VARCHAR,
    EMAIL VARCHAR,
    BUSINESSCONT VARCHAR,
    CONTENT VARCHAR,
    ORGID VARCHAR,
    DEPTID VARCHAR,
    INFOSTATE VARCHAR,
    DIRECTORID VARCHAR,
    ISBUSINESS VARCHAR,
    IS_SUPP VARCHAR,
    ISCLIENTS VARCHAR,
    ISLOGISTICS VARCHAR,
    LEVELID VARCHAR,
    ISABANDON VARCHAR,
    REASON VARCHAR,
    BEACTIVE VARCHAR,
    DOCLEVID BIGINT,
    CREATERID VARCHAR,
    CREATERINFO VARCHAR,
    DELTIME VARCHAR,
    DELUSERID VARCHAR,
    LASTMODIFYTIME VARCHAR,
    ISPASS VARCHAR,
    GROUPID VARCHAR,
    CHANCEID VARCHAR,
    ZONEID VARCHAR,
    CREATETIME VARCHAR,
    EXPAUTORG VARCHAR,
    RESAUTORG VARCHAR,
    ISPARTNER VARCHAR,
    LASTDATE VARCHAR,
    CREATEDATE VARCHAR,
    YINGYZZ VARCHAR,
    ACCTNAME VARCHAR,
    ORGCERT VARCHAR,
    ORGCERTDATE VARCHAR,
    ORGCERTVAL VARCHAR,
    REGYEARDATE VARCHAR,
    GPSADD VARCHAR,
    CENTID VARCHAR,
    ROUTEID BIGINT,
    ROUTEORDER BIGINT,
    LASTSTFID VARCHAR,
    ISMFC VARCHAR,
    BILLCODE VARCHAR,
    ACTBALANCE DECIMAL(18,4),
    POWERBALANCE DECIMAL(18,4),
    RPTYEAR BIGINT,
    QADIRNAME VARCHAR,
    ING VARCHAR,
    LAT VARCHAR,
    SYNCID VARCHAR,
    KOUL DECIMAL(18,4),
    LASTVISITTIME VARCHAR,
    LASTCARETIME VARCHAR,
    LASTSERVTIME VARCHAR,
    LASTRTNTIME VARCHAR,
    CUSNATID VARCHAR,
    CUSSUBID VARCHAR,
    FAX VARCHAR,
    REGAUTHORITY VARCHAR,
    REGDATES VARCHAR,
    REGVALDATE VARCHAR,
    SUBBRANCH VARCHAR,
    CZONEID VARCHAR,
    ISFREEZE VARCHAR,
    REGCAPITAL VARCHAR,
    OPERMODE VARCHAR,
    CAPACITY VARCHAR,
    REGNO VARCHAR,
    LEGALREP VARCHAR,
    IDCARD VARCHAR,
    SELATTACHID VARCHAR,
    CANTON VARCHAR,
    CITY VARCHAR,
    COUNTY VARCHAR,
    BANKACCOUNT VARCHAR,
    ADDRESS VARCHAR,
    BUSISCALE VARCHAR,
    LKINDCONT VARCHAR,
    BUSISCOPE VARCHAR,
    CERTBUSINESSCONT VARCHAR,
    ENTRES VARCHAR,
    CERTSTATUS BIGINT,
    REMARK VARCHAR,
    K_KDGS VARCHAR,
    RENT DECIMAL(18,4),
    K_PZBH VARCHAR,
    K_DRUGTRACE_OWNERID VARCHAR,
    K_DRUGTRACE_ENTID VARCHAR,
    K_DRUGTRACE_REFENTID VARCHAR
);

-- ============================================
-- raw.GOODSATTR - 商品属性扩展表 (5.1万行, 280列)
-- ============================================
CREATE TABLE IF NOT EXISTS raw.GOODSATTR (
    GOODSID VARCHAR,
    ENTID VARCHAR,
    ISMATERIEL VARCHAR,
    ISAUXIL VARCHAR,
    ISTOOLS VARCHAR,
    ISASSETS VARCHAR,
    ISSERV VARCHAR,
    ISFREE VARCHAR,
    ISGIFT VARCHAR,
    ISSTOCKS VARCHAR,
    HESID VARCHAR,
    ACCTLEVEL VARCHAR,
    ANGLETYPE BIGINT,
    SCHEMAID VARCHAR,
    YEARSNUM BIGINT,
    SEASON VARCHAR,
    FABRIC VARCHAR,
    ACCES VARCHAR,
    EXITDATE VARCHAR,
    LIFETYPE VARCHAR,
    CONVRATIO DECIMAL(18,4),
    INTAXRATE DECIMAL(18,4),
    OUTTAXRATE DECIMAL(18,4),
    PURP DECIMAL(18,4),
    PURTAXP DECIMAL(18,4),
    SALEP DECIMAL(18,4),
    SALETAXP DECIMAL(18,4),
    RETAILP DECIMAL(18,4),
    KEMU VARCHAR,
    ISGOODS VARCHAR,
    REMARK VARCHAR,
    ISALWUNDINPACK VARCHAR,
    GOODSGROUP BIGINT,
    ISPUROK VARCHAR,
    ARCHNO VARCHAR,
    MAINLEVEL BIGINT,
    TREATCATE VARCHAR,
    GMPNO VARCHAR,
    GMPVALDATE VARCHAR,
    ISMEMDEDRATE VARCHAR,
    ISADULTTIN VARCHAR,
    ISRFQ VARCHAR,
    SAFEDAYS BIGINT,
    ISDOPE VARCHAR,
    LASTMODIFYTIME VARCHAR,
    BEACTIVE VARCHAR,
    MEDCARETYPE VARCHAR,
    ISTWOVOTE VARCHAR,
    RECIPETYPE VARCHAR,
    INSTREGNO VARCHAR,
    INSTREGVALDATE VARCHAR,
    MFCREGNO VARCHAR,
    MFCREGVALDATE VARCHAR,
    REGNO VARCHAR,
    REGVALDATE VARCHAR,
    RPTYEAR BIGINT,
    REGYEARDATE VARCHAR,
    CATONE BIGINT,
    CATTWO BIGINT,
    CATTHREE BIGINT,
    RTNFLAG BIGINT,
    OPCAT BIGINT,
    MARKETCAT BIGINT,
    SOURCECAT BIGINT,
    ISNECE VARCHAR,
    MULPOINT DECIMAL(18,4),
    ISBRAND VARCHAR,
    ISWEIGHT VARCHAR,
    SALETYPE VARCHAR,
    ISANGLE VARCHAR,
    APPEARDATE VARCHAR,
    BILLNO BIGINT,
    ISPASS VARCHAR,
    GENERALNAME VARCHAR,
    CHEMNAME VARCHAR,
    COMMNAME VARCHAR,
    GCATEGORY VARCHAR,
    ISGSP VARCHAR,
    ISGMP VARCHAR,
    ISFIRST VARCHAR,
    ISHERBAL VARCHAR,
    ISTRAD VARCHAR,
    APPROVALNO VARCHAR,
    FORMULA VARCHAR,
    ISPSY VARCHAR,
    ISMEDICARE VARCHAR,
    ISREFRIG VARCHAR,
    BIDDPRICE DECIMAL(18,4),
    EXPRICE DECIMAL(18,4),
    QUALSTAND VARCHAR,
    ISSALE VARCHAR,
    ISPROCUR VARCHAR,
    ISCONTROL VARCHAR,
    ISMAIN VARCHAR,
    ISPROM VARCHAR,
    LIMITPURP DECIMAL(18,4),
    MAXSALEP DECIMAL(18,4),
    MINSALEP DECIMAL(18,4),
    MAXRETAIP DECIMAL(18,4),
    MINRETAIP DECIMAL(18,4),
    ADRETAILP DECIMAL(18,4),
    MEMPRICE DECIMAL(18,4),
    MANAGEPACK VARCHAR,
    ISCOSTACCT VARCHAR,
    INTERSCA DECIMAL(18,4),
    GENDER VARCHAR,
    PLANCATE BIGINT,
    ISLIMIT VARCHAR,
    LIMITNUM DECIMAL(18,4),
    ISPRES VARCHAR,
    APPROVALDATE VARCHAR,
    APPROVALTO VARCHAR,
    ISDRUG VARCHAR,
    ISKEF VARCHAR,
    ISQA VARCHAR,
    INEFFECTDAY BIGINT,
    DAYUNIT BIGINT,
    DFTWHID VARCHAR,
    DFTLOCATID VARCHAR,
    CURCYCLE BIGINT,
    ISTJ VARCHAR,
    TJ DECIMAL(18,4),
    STORAGETERM VARCHAR,
    ISELEC VARCHAR,
    ISDOUCHK VARCHAR,
    ISWITHPIC VARCHAR,
    REGMARK VARCHAR,
    DOWNPURP DECIMAL(18,4),
    LISTPRICE DECIMAL(18,4),
    QCATEGORY VARCHAR,
    INNERPRICE DECIMAL(18,4),
    REGMARKVALTO VARCHAR,
    ISBASEMED VARCHAR,
    SDATE VARCHAR,
    EDATE VARCHAR,
    WMSMEAS DECIMAL(18,4),
    LISTSALEP DECIMAL(18,4),
    ISWHOLLIMT VARCHAR,
    WHOLLIMTNUM DECIMAL(18,4),
    ISEPHE VARCHAR,
    ISCHKPACK VARCHAR,
    CURRENCYID VARCHAR,
    PAIJIA DECIMAL(18,4),
    ISPOINT VARCHAR,
    ISDEDRATE VARCHAR,
    DEDUCTTYPE BIGINT,
    DEDUCTRATE DECIMAL(18,4),
    ISCHRONIC VARCHAR,
    ELECGOODSCODE VARCHAR,
    ISPREG VARCHAR,
    TMRULEID VARCHAR,
    ISFORBCASH VARCHAR,
    PRESLIMIT DECIMAL(18,4),
    ISMEMPROM VARCHAR,
    MARKETBY VARCHAR,
    STORCAT VARCHAR,
    STORABC VARCHAR,
    STORSCAMAX DECIMAL(18,4),
    STORSCAMIN DECIMAL(18,4),
    ISYS VARCHAR,
    LEVELID VARCHAR,
    ISCENPUR VARCHAR,
    CENPURMODE BIGINT,
    PRCONLEV DECIMAL(18,4),
    ISAID VARCHAR,
    ISFOURKIND VARCHAR,
    STANDCODE VARCHAR,
    STATCATE VARCHAR,
    INSTREGDATE VARCHAR,
    KINDCODE VARCHAR,
    KINDLCODE VARCHAR,
    NKINDCODE VARCHAR,
    NKINDLCODE VARCHAR,
    MDLEVELNAME VARCHAR,
    PREPNSEPC DECIMAL(18,4),
    INSTBY VARCHAR,
    STORAGEMEMO VARCHAR,
    TEMPLATEID VARCHAR,
    TAXCATE BIGINT,
    TAXNAME VARCHAR,
    TAXCODE VARCHAR,
    TPLDOCID VARCHAR,
    ISBREAKABLE VARCHAR,
    FIRSTSUPPNAME VARCHAR,
    ISMEDEQU VARCHAR,
    ISNAILBOX VARCHAR,
    ISSERIAL VARCHAR,
    TRANSTERM VARCHAR,
    GSRCCATE BIGINT,
    ISMEDTOOL VARCHAR,
    ISHIGHVAL VARCHAR,
    TRANSTYPE BIGINT,
    PREPNUNIT VARCHAR,
    MARKETBYADD VARCHAR,
    ISBASENOELEC VARCHAR,
    CATRULE VARCHAR,
    ISFOCAL VARCHAR,
    K_ZBZJS DECIMAL(18,4),
    K_WMSGROUP VARCHAR,
    K_ZCPJCODE VARCHAR,
    K_ISSHS VARCHAR,
    K_ISPACK VARCHAR,
    K_ISZCPJ VARCHAR,
    GOODSPURPOSE VARCHAR,
    BZJS DECIMAL(18,4),
    K_INTAXRATE DECIMAL(18,4),
    K_OUTTAXRATE DECIMAL(18,4),
    K_SWFLBM VARCHAR,
    FIRSTSUPP VARCHAR,
    TRAITS VARCHAR,
    ISDRUGYP VARCHAR,
    ISPUREPIN VARCHAR,
    ISCONTROLPIN VARCHAR,
    ISCIRCULATE VARCHAR,
    ISOTC VARCHAR,
    ISDTP VARCHAR,
    ISGENERICDRUG VARCHAR,
    ISCHRONID VARCHAR,
    YSFHDATE VARCHAR,
    K_ISKJ VARCHAR,
    K_ZBZ DECIMAL(18,4),
    K_ISTXYXQ VARCHAR,
    LCLGROUP VARCHAR,
    FCLGROUP VARCHAR,
    IS_WMS VARCHAR,
    K_CWGM VARCHAR,
    SPBH VARCHAR,
    SPMCH VARCHAR,
    SPLB VARCHAR,
    K_ISQH VARCHAR,
    K_ISSQBB VARCHAR,
    K_ISYYBB VARCHAR,
    K_ISSQJP VARCHAR,
    K_ISYYJP VARCHAR,
    K_ISTH VARCHAR,
    K_ISYFJY VARCHAR,
    K_ISETJY VARCHAR,
    K_ISDBTH VARCHAR,
    K_ISBDB VARCHAR,
    K_ISYWF VARCHAR,
    K_CGSX VARCHAR,
    K_XDSX VARCHAR,
    K_DSPT VARCHAR,
    ONECATNAME VARCHAR,
    TWOCATNAME VARCHAR,
    THREECATNAME VARCHAR,
    FOURCATNAME VARCHAR,
    MEDCARECODE VARCHAR,
    MEDCARENAME VARCHAR,
    K_ISLONG VARCHAR,
    K_XZYY VARCHAR,
    K_TS DECIMAL(18,4),
    K_PZCGY VARCHAR,
    STORATTENTION VARCHAR,
    K_ISYPBZ VARCHAR,
    K_ISSMS VARCHAR,
    K_XSLB VARCHAR,
    ISCJPZ VARCHAR,
    CK_RATE DECIMAL(18,4),
    K_ISHXPZ VARCHAR,
    JFBL DECIMAL(18,4),
    ISHYRZK VARCHAR,
    JGCL VARCHAR,
    K_WLLB VARCHAR,
    K_JYSX VARCHAR,
    FORMULA_DL VARCHAR,
    K_ISJJSP VARCHAR,
    K_ISFML VARCHAR,
    K_ISLD VARCHAR,
    OUTPRICE DECIMAL(18,4),
    OUTTAXPRICE DECIMAL(18,4),
    K_ISTJ VARCHAR,
    YSB_PRICE DECIMAL(18,4),
    YBM_PRICE DECIMAL(18,4),
    YYC_PRICE DECIMAL(18,4),
    YJJ_PRICE DECIMAL(18,4),
    K_XSQD VARCHAR,
    OUTBL DECIMAL(18,4),
    K_GWJ DECIMAL(18,4),
    FIVECATNAME VARCHAR
);

-- ============================================
-- raw.IMEIHIS - IMEI 历史记录表 (1.6万行, 43列)
-- ============================================
CREATE TABLE IF NOT EXISTS raw.IMEIHIS (
    SERIALNUM BIGINT,
    ENTID VARCHAR,
    RANDOMID BIGINT,
    OLDSTATE VARCHAR,
    NEWSTATE VARCHAR,
    BILLNO BIGINT,
    RULEID VARCHAR,
    BUSIATTR VARCHAR,
    BILLSN BIGINT,
    BILLCODE VARCHAR,
    ORGID VARCHAR,
    IMEINO VARCHAR,
    RFID VARCHAR,
    RFTYPE VARCHAR,
    MEAS DECIMAL(18,4),
    TYPESIGN BIGINT,
    RPTBILLNO BIGINT,
    RPTRULEID VARCHAR,
    RPTBILLCODE VARCHAR,
    RPTBILLSN BIGINT,
    RPTSTATUS BIGINT,
    RPTELECTYPE VARCHAR,
    SYSDATES VARCHAR,
    ONTIME VARCHAR,
    BASENUM DECIMAL(18,4),
    PACKID VARCHAR,
    UNIT VARCHAR,
    UDICODE VARCHAR,
    OWNERID VARCHAR,
    SALEMANNAME VARCHAR,
    OPPCONTACT VARCHAR,
    DATES VARCHAR,
    RFNAME VARCHAR,
    GOODSID VARCHAR,
    ANGLEID VARCHAR,
    BATCHCODE VARCHAR,
    PELECCODE VARCHAR,
    ISSUBLINK VARCHAR,
    CODELEVEL VARCHAR,
    PHYSICTYPE BIGINT,
    PKGRATIO VARCHAR,
    IMEISTATE VARCHAR,
    IMEIDESC VARCHAR
);

-- ============================================
-- raw.K_PRN_SENDLOG - 打印/发送日志表 (1.9万行, 18列)
-- ============================================
CREATE TABLE IF NOT EXISTS raw.K_PRN_SENDLOG (
    GUID VARCHAR,
    CREATETIME VARCHAR,
    ORDERID VARCHAR,
    BILLNO VARCHAR,
    KDNORDERCODE VARCHAR,
    SHIPPERCODE VARCHAR,
    LOGISTICCODE VARCHAR,
    CONTENT VARCHAR,
    ISEND VARCHAR,
    DHTYPE VARCHAR,
    MARKDESTINATION VARCHAR,
    ORIGINNAME VARCHAR,
    PACKAGENAME VARCHAR,
    ORIGINCODE VARCHAR,
    PACKAGECODE VARCHAR,
    SORTINGCODE VARCHAR,
    DESTINATIONAME VARCHAR,
    DESTINATIOCODE VARCHAR
);

-- ============================================
-- raw.SKWMS_JGM_W - 仓库JGM表 (147.5万行, 10列)
-- ============================================
CREATE TABLE IF NOT EXISTS raw.SKWMS_JGM_W (
    TRAN_TYPE VARCHAR,
    TRAN_CODE VARCHAR,
    TRAN_SN BIGINT,
    GOODSID VARCHAR,
    BATCHCODE VARCHAR,
    IMEINO VARCHAR,
    ZT BIGINT,
    ID BIGINT,
    TRAN_BILLNO BIGINT,
    BENDIAN VARCHAR
);

-- ============================================
-- raw.ZZERP_CK_KPD_W - 出库开票单 (83.4万行, 57列)
-- ============================================
CREATE TABLE IF NOT EXISTS raw.ZZERP_CK_KPD_W (
    ENTID VARCHAR,
    BILLNO BIGINT,
    BILLCODE VARCHAR,
    BILLSN BIGINT,
    DATES VARCHAR,
    ONTIME VARCHAR,
    OWNERID VARCHAR,
    BUSINESSID VARCHAR,
    OPERATOR VARCHAR,
    DEPT VARCHAR,
    STAFF VARCHAR,
    GOODSID VARCHAR,
    ANGLEID VARCHAR,
    BATCHCODE VARCHAR,
    NUM DECIMAL(18,4),
    CANCELNUM DECIMAL(18,4),
    TAXPRICE DECIMAL(18,4),
    TAXAMOUNT DECIMAL(18,4),
    UNIT VARCHAR,
    PRODUCEDATE VARCHAR,
    VALDATE VARCHAR,
    SUMMARIES VARCHAR,
    REMARK VARCHAR,
    SUPERBILLNO BIGINT,
    SUPERBILLCODE VARCHAR,
    SUPERBILLSN BIGINT,
    FCLS DECIMAL(18,4),
    LCLS DECIMAL(18,4),
    STOREFLAG BIGINT,
    DESIGNATED VARCHAR,
    TWOINVOICE VARCHAR,
    URGENCY VARCHAR,
    STORETYPE VARCHAR,
    PLATCODE VARCHAR,
    SHIPPER VARCHAR,
    AUDITUSER VARCHAR,
    SECAUDITUSER VARCHAR,
    CREATETIME VARCHAR,
    LASTMODIFYTIME VARCHAR,
    ZT BIGINT,
    FHTBH VARCHAR,
    OMS_DJBH VARCHAR,
    LOGISTICSNAME VARCHAR,
    LOGISTICSNO VARCHAR,
    LOGISTICSCODE VARCHAR,
    DS_BW VARCHAR,
    SIGNATURE VARCHAR,
    TEMPLATEURL VARCHAR,
    REFOID VARCHAR,
    BENDIAN VARCHAR,
    LSS DECIMAL(18,4),
    DS_ZCW DECIMAL(18,4),
    CH_ZT BIGINT,
    DJTYPE VARCHAR,
    ZSHL DECIMAL(18,4),
    BQTM VARCHAR,
    XLH VARCHAR
);

-- ============================================
-- raw.ZZERP_GT_KPD_W - 柜台开票单 (747行, 43列)
-- ============================================
CREATE TABLE IF NOT EXISTS raw.ZZERP_GT_KPD_W (
    ENTID VARCHAR,
    BILLNO BIGINT,
    BILLCODE VARCHAR,
    BILLSN BIGINT,
    DATES VARCHAR,
    ONTIME VARCHAR,
    OWNERID VARCHAR,
    BUSINESSID VARCHAR,
    OPERATOR VARCHAR,
    DEPT VARCHAR,
    STAFF VARCHAR,
    GOODSID VARCHAR,
    ANGLEID VARCHAR,
    BATCHCODE VARCHAR,
    NUM DECIMAL(18,4),
    CANCELNUM DECIMAL(18,4),
    TAXPRICE DECIMAL(18,4),
    TAXAMOUNT DECIMAL(18,4),
    UNIT VARCHAR,
    PRODUCEDATE VARCHAR,
    VALDATE VARCHAR,
    SUMMARIES VARCHAR,
    REMARK VARCHAR,
    SUPERBILLNO BIGINT,
    SUPERBILLCODE VARCHAR,
    SUPERBILLSN BIGINT,
    FCLS DECIMAL(18,4),
    LCLS DECIMAL(18,4),
    STOREFLAG BIGINT,
    DESIGNATED VARCHAR,
    TWOINVOICE VARCHAR,
    URGENCY VARCHAR,
    STORETYPE VARCHAR,
    PLATCODE VARCHAR,
    SHIPPER VARCHAR,
    AUDITUSER VARCHAR,
    SECAUDITUSER VARCHAR,
    CREATETIME VARCHAR,
    LASTMODIFYTIME VARCHAR,
    ZT BIGINT,
    CH_ZT BIGINT,
    ZSHL DECIMAL(18,4),
    XLH VARCHAR
);

-- ============================================
-- raw.ZZERP_RK_KPD_W - 入库开票单 (1.8万行, 35列)
-- ============================================
CREATE TABLE IF NOT EXISTS raw.ZZERP_RK_KPD_W (
    ENTID VARCHAR,
    BILLNO BIGINT,
    BILLCODE VARCHAR,
    BILLSN BIGINT,
    DATES VARCHAR,
    ONTIME VARCHAR,
    OWNERID VARCHAR,
    BUSINESSID VARCHAR,
    OPERATOR VARCHAR,
    DEPT VARCHAR,
    STAFF VARCHAR,
    GOODSID VARCHAR,
    ANGLEID VARCHAR,
    BATCHCODE VARCHAR,
    NUM DECIMAL(18,4),
    CANCELNUM DECIMAL(18,4),
    TAXPRICE DECIMAL(18,4),
    TAXAMOUNT DECIMAL(18,4),
    UNIT VARCHAR,
    PRODUCEDATE VARCHAR,
    VALDATE VARCHAR,
    SUMMARIES VARCHAR,
    REMARK VARCHAR,
    SUPERBILLNO BIGINT,
    SUPERBILLCODE VARCHAR,
    SUPERBILLSN BIGINT,
    STOREFLAG BIGINT,
    DESIGNATED VARCHAR,
    TWOINVOICE VARCHAR,
    URGENCY VARCHAR,
    LCLCODE VARCHAR,
    CREATETIME VARCHAR,
    LASTMODIFYTIME VARCHAR,
    ZT BIGINT,
    YANSHR VARCHAR
);

-- ============================================
-- raw.ZZERP_XT_KPD_W - 系统开票单 (1.5万行, 40列)
-- ============================================
CREATE TABLE IF NOT EXISTS raw.ZZERP_XT_KPD_W (
    ENTID VARCHAR,
    BILLNO BIGINT,
    BILLCODE VARCHAR,
    BILLSN BIGINT,
    DATES VARCHAR,
    ONTIME VARCHAR,
    OWNERID VARCHAR,
    BUSINESSID VARCHAR,
    OPERATOR VARCHAR,
    DEPT VARCHAR,
    STAFF VARCHAR,
    GOODSID VARCHAR,
    ANGLEID VARCHAR,
    BATCHCODE VARCHAR,
    NUM DECIMAL(18,4),
    CANCELNUM DECIMAL(18,4),
    TAXPRICE DECIMAL(18,4),
    TAXAMOUNT DECIMAL(18,4),
    UNIT VARCHAR,
    PRODUCEDATE VARCHAR,
    VALDATE VARCHAR,
    SUMMARIES VARCHAR,
    REMARK VARCHAR,
    SUPERBILLNO BIGINT,
    SUPERBILLCODE VARCHAR,
    SUPERBILLSN BIGINT,
    STOREFLAG BIGINT,
    DESIGNATED VARCHAR,
    TWOINVOICE VARCHAR,
    URGENCY VARCHAR,
    LCLCODE VARCHAR,
    CREATETIME VARCHAR,
    LASTMODIFYTIME VARCHAR,
    ZT BIGINT,
    YANSHR VARCHAR,
    OMS_DJBH VARCHAR,
    LOGISTICSCODE VARCHAR,
    LOGISTICSNAME VARCHAR,
    LOGISTICSNO VARCHAR,
    BENDIAN VARCHAR
);
