-- ============================================
-- 补充建表 SQL - 6 张新增权限表
-- 采购退货 / 销售退货 / 调拨
-- 2026-08-04
-- ============================================

-- ============================================
-- raw.RETBILLMT - 采购退货主表 (23.5万行, 89列)
-- ============================================
CREATE TABLE IF NOT EXISTS raw.RETBILLMT (
    ENTID VARCHAR,
    BILLNO BIGINT,
    BILLCODE VARCHAR,
    SMALLNO VARCHAR,
    BUSITYPE BIGINT,
    RULEID VARCHAR,
    DATES VARCHAR,
    ONTIME VARCHAR,
    WETHERID VARCHAR,
    ONHOUR BIGINT,
    SYSDATES VARCHAR,
    CONTACTID VARCHAR,
    CURRENCYID VARCHAR,
    PAIJIA DECIMAL(18,5),
    RECAMT DECIMAL(18,2),
    FUNAMT DECIMAL(18,2),
    PAIDINAMT DECIMAL(18,2),
    DISAMT DECIMAL(18,2),
    WIPAMT DECIMAL(18,2),
    CHAAMT DECIMAL(18,2),
    DTCOUNT BIGINT,
    AMTCOUNT BIGINT,
    COPIES BIGINT,
    CAOZY VARCHAR,
    DEPTID VARCHAR,
    ORGID VARCHAR,
    REMARK VARCHAR,
    SUMMARIES VARCHAR,
    COSTAMT DECIMAL(18,2),
    PROFIT DECIMAL(18,2),
    AMOUNT DECIMAL(18,2),
    STATIONCODE VARCHAR,
    ARRDAYID VARCHAR,
    GUARDCODE BIGINT,
    GUARDSN BIGINT,
    TAX DECIMAL(18,2),
    CARDID BIGINT,
    DOCTOR VARCHAR,
    TAXAMOUNT DECIMAL(18,2),
    SAVEAMT DECIMAL(18,2),
    DOCTORID VARCHAR,
    BALANCE DECIMAL(18,2),
    OLDBALANCE DECIMAL(18,2),
    POINT DECIMAL(18,2),
    OLDPOINT DECIMAL(18,2),
    CONTACT VARCHAR,
    IDCARD VARCHAR,
    TELEPHONE VARCHAR,
    MOBILE VARCHAR,
    REBILLNO BIGINT,
    REBILLCODE VARCHAR,
    RERULEID VARCHAR,
    RFBILLNO BIGINT,
    RFBILLCODE VARCHAR,
    RFRULEID VARCHAR,
    MICBILLCODE VARCHAR,
    CARDSRC BIGINT,
    TRAAMT DECIMAL(18,2),
    ZHRAMT DECIMAL(18,2),
    TOKENAMT DECIMAL(18,2),
    POTAMT DECIMAL(18,2),
    INVOICEAMT DECIMAL(18,2),
    ISINVOICE VARCHAR,
    PROMID VARCHAR,
    PRICEPROMID VARCHAR,
    HOMECONID VARCHAR,
    TREATDOCTID VARCHAR,
    TREATASSID VARCHAR,
    TREATDEPTID VARCHAR,
    GHBILLNO BIGINT,
    GHBILLCODE VARCHAR,
    MICBILLDATE VARCHAR,
    ACTCOSTAMT DECIMAL(18,2),
    ACTPROFIT DECIMAL(18,2),
    SETL_ID VARCHAR,
    MDTRT_CERT_TYPE VARCHAR,
    PAYCAT BIGINT,
    EXBILLNO BIGINT,
    ECSHOPORDER VARCHAR,
    ECSHOPORDER_OLD VARCHAR,
    ECSHOPJHCODE VARCHAR,
    ECSHOPBILLOCDE VARCHAR,
    ECSHOPCODE VARCHAR,
    ECSHOPNAME VARCHAR,
    LOGISTICCODE VARCHAR,
    LOGISTICSNAME VARCHAR,
    LOGISTICSCODE VARCHAR,
    ISPRES VARCHAR,
    ISELEC VARCHAR
);

-- ============================================
-- raw.RETGOODSDT - 采购退货明细表 (33万行, 54列)
-- ============================================
CREATE TABLE IF NOT EXISTS raw.RETGOODSDT (
    BILLSN BIGINT,
    ENTID VARCHAR,
    BILLNO BIGINT,
    BILLSORT BIGINT,
    SALEMANID VARCHAR,
    WHID VARCHAR,
    GOODSID VARCHAR,
    PACKID VARCHAR,
    UNIT VARCHAR,
    MEAS BIGINT,
    ANGLEID VARCHAR,
    BASENUM DECIMAL(18,2),
    NUM DECIMAL(18,2),
    RETAILP DECIMAL(18,4),
    TRAPRICE DECIMAL(18,4),
    RECAMT DECIMAL(18,2),
    PAIDINAMT DECIMAL(18,2),
    DEDRATE DECIMAL(18,2),
    AMOUNT DECIMAL(18,2),
    RATE DECIMAL(18,2),
    TAX DECIMAL(18,2),
    COST DECIMAL(18,6),
    COSTAMT DECIMAL(18,2),
    PROFIT DECIMAL(18,2),
    CHECKCOST DECIMAL(18,6),
    ISFREE VARCHAR,
    REMARK VARCHAR,
    TEAM BIGINT,
    OWNERID VARCHAR,
    PROMID VARCHAR,
    DEPTID VARCHAR,
    LOCATID VARCHAR,
    ISMEMSALE VARCHAR,
    DISAVGAMT DECIMAL(18,2),
    WIPAVGAMT DECIMAL(18,2),
    PRICEPROMID VARCHAR,
    TRAAMT DECIMAL(18,2),
    ZHRAVGAMT DECIMAL(18,2),
    TOKENAVGAMT DECIMAL(18,2),
    POTAVGAMT DECIMAL(18,2),
    REBILLNO BIGINT,
    REBILLCODE VARCHAR,
    RERULEID VARCHAR,
    REBILLSN BIGINT,
    SUPPLIERSID VARCHAR,
    ACTCOSTAMT DECIMAL(18,2),
    ACTPROFIT DECIMAL(18,2),
    PASSUSERID VARCHAR,
    PASSEXPID VARCHAR,
    ECSHOPBILLSN BIGINT,
    ECSHOPORDER VARCHAR,
    ECSHOPORDER_OLD VARCHAR,
    ECSHOPBILLOCDE VARCHAR,
    IMEINOLIST VARCHAR
);

-- ============================================
-- raw.SALERTNMT - 销售退货主表 (244行, 43列)
-- ============================================
CREATE TABLE IF NOT EXISTS raw.SALERTNMT (
    BILLNO BIGINT,
    ENTID VARCHAR,
    BILLCODE VARCHAR,
    RULEID VARCHAR,
    DATES VARCHAR,
    ONTIME VARCHAR,
    SYSDATES VARCHAR,
    ISVAT VARCHAR,
    CLIENTID VARCHAR,
    DEDRATE DECIMAL(18,2),
    SALECLIENTID VARCHAR,
    ACCPTID VARCHAR,
    ADDRESS VARCHAR,
    CURRENCYID VARCHAR,
    PAIJIA DECIMAL(18,5),
    PAYORGID VARCHAR,
    SALEMANID VARCHAR,
    CAOZY VARCHAR,
    ORGID VARCHAR,
    DEPTID VARCHAR,
    CONTRACTCODE VARCHAR,
    INVOICE VARCHAR,
    ISEND VARCHAR,
    ISDONE VARCHAR,
    BILLSTATE BIGINT,
    SUMMARIES VARCHAR,
    REMARK VARCHAR,
    OPPCONTID VARCHAR,
    PAYTYPE VARCHAR,
    WLDEPTID VARCHAR,
    SREASON VARCHAR,
    TPLBILLID VARCHAR,
    TPLDOCID VARCHAR,
    BUSINESSNAME VARCHAR,
    BUSICATE BIGINT,
    IS_WMS VARCHAR,
    B2BCODE VARCHAR,
    ORDERID VARCHAR,
    POSNAME VARCHAR,
    REFOID VARCHAR,
    DSTYPE VARCHAR,
    DSCODE VARCHAR,
    APID VARCHAR
);

-- ============================================
-- raw.SALERTNDT - 销售退货明细表 (259行, 53列)
-- ============================================
CREATE TABLE IF NOT EXISTS raw.SALERTNDT (
    BILLNO BIGINT,
    BILLSN BIGINT,
    ENTID VARCHAR,
    BILLSORT BIGINT,
    GOODSID VARCHAR,
    OWNERID VARCHAR,
    WHORGID VARCHAR,
    PACKID VARCHAR,
    UNIT VARCHAR,
    MEAS BIGINT,
    ANGLEID VARCHAR,
    BASENUM DECIMAL(18,2),
    NUM DECIMAL(18,2),
    EXECNUM DECIMAL(18,2),
    CANCELNUM DECIMAL(18,2),
    PRICE DECIMAL(18,6),
    TAXPRICE DECIMAL(18,4),
    DEDRATE DECIMAL(18,2),
    BASEPRICE DECIMAL(18,6),
    BASETAXPRICE DECIMAL(18,4),
    AMOUNT DECIMAL(18,2),
    RATE DECIMAL(18,2),
    TAX DECIMAL(18,2),
    TAXAMOUNT DECIMAL(18,2),
    RETAILP DECIMAL(18,4),
    ISFREE VARCHAR,
    ISDONE VARCHAR,
    REMARK VARCHAR,
    WHID VARCHAR,
    LOCATID VARCHAR,
    COST DECIMAL(18,6),
    REBILLCODE VARCHAR,
    REBILLSN BIGINT,
    RERULEID VARCHAR,
    REBILLNO BIGINT,
    CONTRACTID VARCHAR,
    APID VARCHAR,
    SUPPLIERSID VARCHAR,
    BATCHNO BIGINT,
    TPLROWID VARCHAR,
    TPLDOCID VARCHAR,
    GOODSCODE VARCHAR,
    GOODSNAME VARCHAR,
    GOODSSPEC VARCHAR,
    PLACE VARCHAR,
    MANUFACTURER VARCHAR,
    GOODSREF VARCHAR,
    GENERALNAME VARCHAR,
    APPROVALNO VARCHAR,
    MARKETBY VARCHAR,
    INSTREGNO VARCHAR,
    INSTBY VARCHAR,
    DS_PRICE DECIMAL(18,2)
);

-- ============================================
-- raw.TRBILLMT - 调拨/移库主表 (78行, 86列)
-- ============================================
CREATE TABLE IF NOT EXISTS raw.TRBILLMT (
    ENTID VARCHAR,
    BILLNO BIGINT,
    BILLCODE VARCHAR,
    SMALLNO VARCHAR,
    BUSITYPE BIGINT,
    RULEID VARCHAR,
    DATES VARCHAR,
    ONTIME VARCHAR,
    ONHOUR BIGINT,
    SYSDATES VARCHAR,
    CONTACTID VARCHAR,
    CURRENCYID VARCHAR,
    PAIJIA DECIMAL(18,5),
    RECAMT DECIMAL(18,2),
    FUNAMT DECIMAL(18,2),
    PAIDINAMT DECIMAL(18,2),
    DISAMT DECIMAL(18,2),
    WIPAMT DECIMAL(18,2),
    CHAAMT DECIMAL(18,2),
    DTCOUNT BIGINT,
    AMTCOUNT BIGINT,
    COPIES BIGINT,
    CAOZY VARCHAR,
    DEPTID VARCHAR,
    ORGID VARCHAR,
    REMARK VARCHAR,
    SUMMARIES VARCHAR,
    COSTAMT DECIMAL(18,2),
    PROFIT DECIMAL(18,2),
    AMOUNT DECIMAL(18,2),
    STATIONCODE VARCHAR,
    ARRDAYID VARCHAR,
    GUARDSN BIGINT,
    GUARDCODE BIGINT,
    WETHERID VARCHAR,
    TAX DECIMAL(18,2),
    CARDID BIGINT,
    DOCTOR VARCHAR,
    TAXAMOUNT DECIMAL(18,2),
    SAVEAMT DECIMAL(18,2),
    DOCTORID VARCHAR,
    BALANCE DECIMAL(18,2),
    OLDBALANCE DECIMAL(18,2),
    POINT DECIMAL(18,2),
    OLDPOINT DECIMAL(18,2),
    CONTACT VARCHAR,
    IDCARD VARCHAR,
    TELEPHONE VARCHAR,
    MOBILE VARCHAR,
    REBILLNO BIGINT,
    REBILLCODE VARCHAR,
    RERULEID VARCHAR,
    RFBILLNO BIGINT,
    RFBILLCODE VARCHAR,
    RFRULEID VARCHAR,
    MICBILLCODE VARCHAR,
    CARDSRC BIGINT,
    TRAAMT DECIMAL(18,2),
    ZHRAMT DECIMAL(18,2),
    TOKENAMT DECIMAL(18,2),
    POTAMT DECIMAL(18,2),
    INVOICEAMT DECIMAL(18,2),
    ISINVOICE VARCHAR,
    PROMID VARCHAR,
    PRICEPROMID VARCHAR,
    HOMECONID VARCHAR,
    TREATDOCTID VARCHAR,
    TREATASSID VARCHAR,
    TREATDEPTID VARCHAR,
    GHBILLNO BIGINT,
    GHBILLCODE VARCHAR,
    MICBILLDATE VARCHAR,
    SETL_ID VARCHAR,
    MDTRT_CERT_TYPE VARCHAR,
    PAYCAT BIGINT,
    ECSHOPORDER VARCHAR,
    ECSHOPORDER_OLD VARCHAR,
    ECSHOPJHCODE VARCHAR,
    ECSHOPBILLOCDE VARCHAR,
    ECSHOPCODE VARCHAR,
    ECSHOPNAME VARCHAR,
    LOGISTICCODE VARCHAR,
    LOGISTICSNAME VARCHAR,
    LOGISTICSCODE VARCHAR,
    ISPRES VARCHAR,
    ISELEC VARCHAR
);

-- ============================================
-- raw.TRGOODSDT - 调拨/移库明细表 (78行, 54列)
-- ============================================
CREATE TABLE IF NOT EXISTS raw.TRGOODSDT (
    BILLSN BIGINT,
    ENTID VARCHAR,
    BILLNO BIGINT,
    BILLSORT BIGINT,
    SALEMANID VARCHAR,
    WHID VARCHAR,
    GOODSID VARCHAR,
    PACKID VARCHAR,
    UNIT VARCHAR,
    MEAS BIGINT,
    ANGLEID VARCHAR,
    BASENUM DECIMAL(18,2),
    NUM DECIMAL(18,2),
    RETAILP DECIMAL(18,4),
    TRAPRICE DECIMAL(18,4),
    RECAMT DECIMAL(18,2),
    PAIDINAMT DECIMAL(18,2),
    DEDRATE DECIMAL(18,2),
    AMOUNT DECIMAL(18,2),
    RATE DECIMAL(18,2),
    TAX DECIMAL(18,2),
    COST DECIMAL(18,6),
    COSTAMT DECIMAL(18,2),
    PROFIT DECIMAL(18,2),
    CHECKCOST DECIMAL(18,6),
    ISFREE VARCHAR,
    PROMID VARCHAR,
    REMARK VARCHAR,
    TEAM BIGINT,
    DEPTID VARCHAR,
    OWNERID VARCHAR,
    LOCATID VARCHAR,
    ISMEMSALE VARCHAR,
    DISAVGAMT DECIMAL(18,2),
    WIPAVGAMT DECIMAL(18,2),
    PRICEPROMID VARCHAR,
    TRAAMT DECIMAL(18,2),
    ZHRAVGAMT DECIMAL(18,2),
    TOKENAVGAMT DECIMAL(18,2),
    POTAVGAMT DECIMAL(18,2),
    REBILLNO BIGINT,
    REBILLCODE VARCHAR,
    RERULEID VARCHAR,
    REBILLSN BIGINT,
    SUPPLIERSID VARCHAR,
    ACTCOSTAMT DECIMAL(18,2),
    ACTPROFIT DECIMAL(18,2),
    PASSUSERID VARCHAR,
    PASSEXPID VARCHAR,
    ECSHOPBILLSN BIGINT,
    ECSHOPORDER VARCHAR,
    ECSHOPORDER_OLD VARCHAR,
    ECSHOPBILLOCDE VARCHAR,
    IMEINOLIST VARCHAR
);
